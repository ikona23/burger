<div class = "form">
  <div class = "ponuka_block">
    <h1>Objednávky</h1>
    <table class="tableform">

  <thead>
    <tr>
      <th>Meno</th>
      <th>Email</th>
      <th>Telefon</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.meno %></td>
        <td><%= post.email %></td>
        <td><%= post.telefon %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>

  </div>
</div>